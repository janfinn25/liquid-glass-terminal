#!/bin/bash
# Liquid Glass Terminal Environment
#
# The Liquid Glass.terminal profile does all the heavy lifting:
# - Sets background to deep blue (#25288b) with blur + transparency
# - Maps ALL 16 ANSI color slots to white (#feffff)
# - This makes CLI tools like Claude Code render monochrome on glass
#
# This script exists as a fallback for terminals that don't support
# .terminal profile import (iTerm2, Ghostty, Kitty, etc.)
# It replicates the same effect: all ANSI colors → white on blue glass.

# ─── All ANSI slots → white (matching Liquid Glass.terminal) ─────────

# Normal (0-7)
printf '\033]4;0;rgb:00/00/00\033\\'    #  0 Black (keep black for spacing)
printf '\033]4;1;rgb:fe/ff/ff\033\\'    #  1 Red    → white
printf '\033]4;2;rgb:fe/ff/ff\033\\'    #  2 Green  → white
printf '\033]4;3;rgb:fe/ff/ff\033\\'    #  3 Yellow → white
printf '\033]4;4;rgb:fe/ff/ff\033\\'    #  4 Blue   → white
printf '\033]4;5;rgb:fe/ff/ff\033\\'    #  5 Magenta→ white
printf '\033]4;6;rgb:fe/ff/ff\033\\'    #  6 Cyan   → white
printf '\033]4;7;rgb:fe/ff/ff\033\\'    #  7 White  → white

# Bright (8-15)
printf '\033]4;8;rgb:00/00/00\033\\'    #  8 Bright Black (dim text)
printf '\033]4;9;rgb:fe/ff/ff\033\\'    #  9 Bright Red    → white
printf '\033]4;10;rgb:fe/ff/ff\033\\'   # 10 Bright Green  → white
printf '\033]4;11;rgb:fe/ff/ff\033\\'   # 11 Bright Yellow → white
printf '\033]4;12;rgb:fe/ff/ff\033\\'   # 12 Bright Blue   → white
printf '\033]4;13;rgb:fe/ff/ff\033\\'   # 13 Bright Magenta→ white
printf '\033]4;14;rgb:fe/ff/ff\033\\'   # 14 Bright Cyan   → white
printf '\033]4;15;rgb:fe/ff/ff\033\\'   # 15 Bright White  → white

# ─── Special colors ──────────────────────────────────────────────────
# NOTE: We do NOT set OSC 11 (background) here. The .terminal profile
# handles background color + blur + transparency. OSC 11 would override
# it with a flat opaque color, killing the glass effect.

printf '\033]10;rgb:f5/f1/f1\033\\'     # Foreground text
printf '\033]12;rgb:fe/ff/ff\033\\'     # Cursor

# ─── SGR reset ───────────────────────────────────────────────────────
printf '\033[0m'

echo "🔮 Liquid Glass active"
